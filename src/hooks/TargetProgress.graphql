query GetTargetProgress {
  getAllTargetProgress(courseId: "sample") {
    student,
    target {
      targetName
    }
    objectives {
      objectiveId
      objectiveName
      tasks {
        task {
           name
        }
        mastery
      }
    }
  }
}
query GetTargetProgress {
   getAllTargetProgress(courseId: "sample") {
      student
      target {
         targetName
      }
      objectives {
         objectiveId
         objectiveName
         tasks {
            taskId
            taskName
            mastery
         }
      }
   }
}
